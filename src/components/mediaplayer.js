import React from "react";
import ReactPlayer from "react-player";
const mediaplayer = ({ props }) => {
  console.log("This is the props from the mediaplayer component: ");
  return <ReactPlayer url="" />;
};

export default mediaplayer;
